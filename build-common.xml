<?xml version="1.0" encoding="ISO-8859-1"?>
<project name="build-common" default="default">
    <import file="dependencies.xml"/>

    <target name="default" depends="dist"/>

    <target name="clean">
        <delete dir="${dependencies.basedir}/classes"/>
        <delete dir="${dependencies.basedir}/libs"/>
        <delete dir="${dependencies.basedir}/dist"/>
    </target>

    <target  name="prepare">
            <tstamp/>
        <mkdir dir="${dependencies.basedir}/classes/${ant.project.name}"/>
        <mkdir dir="${dependencies.basedir}/libs"/>
        <mkdir dir="${dependencies.basedir}/dist/${ant.project.name}"/>
    </target>


    <fileset id="externalsSet" dir="${dependencies.basedir}/external/production">
        <include name="*.jar"/>
    </fileset>
    <fileset id="internalsSet" dir="${dependencies.basedir}/libs">
        <include name="*.jar"/>
    </fileset>
    <path id="jars">
        <fileset refid="externalsSet"/>
        <fileset refid="internalsSet"/>
    </path>


    <!-- compile sources -->
    <target name="compile" depends="prepare">
        <javac srcdir="${dependencies.basedir}/modules/${ant.project.name}/src"
               destdir="${dependencies.basedir}/classes/${ant.project.name}">
            <classpath refid="jars"></classpath>
        </javac>

    </target>

    <target name="copy-resources" depends="prepare">
        <copy file="${dependencies.basedir}/modules/${ant.project.name}/manifest.mf"
              todir="${dependencies.basedir}/classes/${ant.project.name}"/>
    </target>

    <target name="create-manifest" depends="copy-resources">
        <pathconvert property="jars-var" pathsep=" " refid="internalsSet">
            <map from="${dependencies.basedir}/libs/" to=""/>
        </pathconvert>
         <echo message="Class-Path: ${jars-var} ${jars-external} ${line.separator}"
               file="${dependencies.basedir}/classes/${ant.project.name}/manifest.mf"
               append="yes"/>
    </target>



    <target name="create-jar" depends="compile,create-manifest">
        <echo message="${ant.project.name} - build-common.dist"/>
        <jar jarfile="${dependencies.basedir}/libs/openCage-${ant.project.name}-${version}.jar"
             basedir="${dependencies.basedir}/classes/${ant.project.name}"
             manifest="${dependencies.basedir}/classes/${ant.project.name}/manifest.mf">
            <fileset dir="${dependencies.basedir}/classes/${ant.project.name}" excludes="**/*test*"/>
        </jar>
    </target>

    <target name="dist" depends="create-jar" >
        <copy todir="${dependencies.basedir}/dist/${ant.project.name}">
            <!--<fileset dir="${deploydir-other}"/>-->
            <fileset dir="${dependencies.basedir}/libs"/>
            <fileset refid="externalsSet"/>
        </copy>

    </target>

    <target name="dist.dependencies">
        <antcall target="depend.${ant.project.name}"/>
    </target>
</project>