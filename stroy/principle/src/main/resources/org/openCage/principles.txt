Project Layout

    maven standard

Class layout

    licence
    class docu
    class

    e.g. VersionEx


Test
    Junit 4

    junit 3 is an ok package, but keep it simple and stick to one.

    test methods are short
    the method name describes what it tests

    e.g. VersionExTest

Documentation

    Write Docu for other people, e.g. you in 3 years

AuditTrail
    Document the decision

    e.g. 2009.05.10
         Maybe deleted
         this is no different from null
         iff null is never used as value

Naming Convention
    findX return X if found otherwise null
    getX return X never returns null (will rather throw)
    hasX return true if X is available